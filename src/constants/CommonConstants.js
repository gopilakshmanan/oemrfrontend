export const MandatoryMsg = () => (
    <h6><span className='text-danger'>*</span> Denotes Required</h6>
)

export const MultiSortTip = () => {
    return <div className="text-center text-muted">
        <em>Tip: Hold shift when sorting to multi-sort!</em>
    </div>
}

export const EncounterFormsDisabledDropdownTip = () => {
    return <div className="text-center text-muted">
        <em>Tip: Saving New Encounter Form enable's adding other forms!</em>
    </div>
}